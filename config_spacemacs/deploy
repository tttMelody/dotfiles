#!/bin/bash - 

if [[ ! -d ~/.spacemacs.d ]]; then
	mkdir ~/.spacemacs.d
fi
ln -sif ~/dotfiles/config_spacemacs/init.el ~/.spacemacs.d/init.el
if [[ ! -L ~/.spacemacs.d/config ]]; then
	ln -sif ~/dotfiles/config_spacemacs/config ~/.spacemacs.d/config
fi

if [[ ! -L ~/.emacs.d/private/dmx ]]; then
	ln -sif ~/dotfiles/config_spacemacs/private_layers/dmx ~/.emacs.d/private/dmx
fi
